// Limpa as imagens do docker e containeres
sudo ./clean_docker_images.sh

// Envia arquivo pra VM
scp <arquivo> univates@177.44.248.8:/home/univates

// Executa o script inicial
sudo ./setup-infra.sh


sudo docker exec -it postgres-db-homolog psql -U postgres -d registro_receitas_db
\dt

sudo docker exec -it postgres-db-prod psql -U postgres -d registro_receitas_db
\dt


  public void naoFazNada(){

  }

